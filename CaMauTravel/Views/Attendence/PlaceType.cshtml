@model List<Model.EF.PlaceType>
@{
    ViewBag.Title = "PlaceType";
}

<ul class="sub-menu">
    @foreach (var item in Model)
    {
        <li><a href="/attraction/@item.MetaTitle-@item.ID">@item.Name</a></li>
    }
</ul>
