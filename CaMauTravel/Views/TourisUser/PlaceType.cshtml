@model List<Model.EF.PlaceType>
@{
    ViewBag.Title = "PlaceType";
}

<ul class="sub-menu">
        @foreach (var item in Model)
        {
            <li><a href="/touris/@item.MetaTitle-@item.ID">@item.Name</a></li>
        }
    </ul>
